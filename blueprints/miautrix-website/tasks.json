[
  {
    "id": "E1-T1",
    "title": "Scaffold Laravel, Livewire, Pest, tooling, Tailwind v4",
    "epic": "01-foundation",
    "dependencies": [],
    "priority": "p0",
    "acceptance": [
      "WHEN `./vendor/bin/pint --test` runs THE SYSTEM SHALL exit 0.",
      "WHEN `./vendor/bin/phpstan analyse` runs THE SYSTEM SHALL exit 0 with zero errors at level 5.",
      "WHEN `./vendor/bin/pest` runs THE SYSTEM SHALL exit 0 with 0 failed and 0 skipped.",
      "WHEN `npm run build` runs THE SYSTEM SHALL exit 0 and emit `public/build/manifest.json`.",
      "WHEN the built server receives a GET to `/` THE SYSTEM SHALL return HTTP 200.",
      "WHEN `composer.json` is inspected THE SYSTEM SHALL contain no `predis/predis` and no `laravel/horizon` entry."
    ],
    "verify": [
      "./vendor/bin/pint --test",
      "./vendor/bin/phpstan analyse",
      "./vendor/bin/pest",
      "npm run build",
      "php artisan serve --port=8123 & SERVE_PID=$!; sleep 1; test \"$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8123/)\" = 200; kill \"$SERVE_PID\"",
      "! grep -q '\"predis/predis\"' composer.json",
      "! grep -q '\"laravel/horizon\"' composer.json"
    ],
    "checkpoint": "step-01-scaffold",
    "files": [
      "composer.json",
      "package.json",
      "vite.config.js",
      "resources/css/app.css",
      ".env.example"
    ],
    "status": "done"
  },
  {
    "id": "E1-T2",
    "title": "Git remote, branches, branch protection via REST API",
    "epic": "01-foundation",
    "dependencies": [
      "E1-T1"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `git remote -v` runs THE SYSTEM SHALL list `origin` pointing at the configured repository URL.",
      "WHEN `main` is pushed THE SYSTEM SHALL be visible at `origin/main`.",
      "WHEN `develop` is pushed THE SYSTEM SHALL be visible at `origin/develop`.",
      "WHEN the branch protection API is queried for `main` THE SYSTEM SHALL report `required_pull_request_reviews` present and `required_status_checks.contexts` containing `ci`.",
      "WHEN an unauthenticated force-push to `main` is attempted THE SYSTEM SHALL be rejected by GitHub."
    ],
    "verify": [
      "git ls-remote origin main",
      "curl -sS -H \"Authorization: Bearer $GITHUB_TOKEN\" -H \"Accept: application/vnd.github+json\" \"https://api.github.com/repos/$GITHUB_OWNER/$GITHUB_REPO/branches/main/protection\" | jq -e --arg ctx \"ci\" '.required_pull_request_reviews != null and (.required_status_checks.contexts | index($ctx)) != null'"
    ],
    "checkpoint": "step-02-git-remote",
    "files": [
      ".git/config"
    ],
    "status": "done"
  },
  {
    "id": "E1-T3",
    "title": "GitHub Actions CI with dependency audits",
    "epic": "01-foundation",
    "dependencies": [
      "E1-T2"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `.github/workflows/ci.yml` is parsed as YAML THE SYSTEM SHALL be syntactically valid.",
      "WHEN the workflow runs on a pushed branch THE SYSTEM SHALL execute `composer audit` and `npm audit --audit-level=high` as distinct steps.",
      "WHEN the workflow runs THE SYSTEM SHALL use a PostgreSQL 18 service container and no Redis service.",
      "WHEN a pushed branch's CI run completes THE SYSTEM SHALL report success (all steps exit 0).",
      "WHEN `composer audit` finds a high-severity advisory THE SYSTEM SHALL fail the workflow (exit non-zero)."
    ],
    "verify": [
      "python3 -c \"import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))\"",
      "grep -q \"composer audit\" .github/workflows/ci.yml",
      "grep -q \"npm audit --audit-level=high\" .github/workflows/ci.yml",
      "! grep -qi \"redis\" .github/workflows/ci.yml",
      "git push origin HEAD:ci-smoke-check && curl -sS -H \"Authorization: Bearer $GITHUB_TOKEN\" \"https://api.github.com/repos/$GITHUB_OWNER/$GITHUB_REPO/commits/$(git rev-parse HEAD)/check-runs\" | jq -e --arg ctx \"ci\" --arg ok \"success\" '[.check_runs[] | select(.name==$ctx)][0].conclusion == $ok' && git push origin --delete ci-smoke-check"
    ],
    "checkpoint": "step-03-ci",
    "files": [
      ".github/workflows/ci.yml"
    ],
    "status": "pending"
  },
  {
    "id": "E1-T4",
    "title": "Provision Debian 13 LXC (Nginx, PHP-FPM, PostgreSQL, supervisor, UFW)",
    "epic": "01-foundation",
    "dependencies": [
      "E1-T3"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN the remote host is queried for `systemctl is-active nginx` THE SYSTEM SHALL report `active`.",
      "WHEN queried for `systemctl is-active php8.4-fpm` THE SYSTEM SHALL report `active`.",
      "WHEN queried for `systemctl is-active postgresql` THE SYSTEM SHALL report `active`.",
      "WHEN queried for `systemctl is-active supervisor` THE SYSTEM SHALL report `active`.",
      "WHEN a TCP connection to the LXC's PostgreSQL port is attempted from OFF-HOST THE SYSTEM SHALL be refused.",
      "WHEN `ufw status` is queried THE SYSTEM SHALL show only 80, 443, and the configured SSH port as ALLOW."
    ],
    "verify": [
      "ssh -i \"$LXC_SSH_KEY\" \"root@$LXC_HOST\" 'systemctl is-active nginx && systemctl is-active php8.4-fpm && systemctl is-active postgresql && systemctl is-active supervisor'",
      "timeout 3 bash -c \"cat < /dev/null > /dev/tcp/$LXC_HOST/5432\" ; test $? -ne 0",
      "test \"$(ssh -i \"$LXC_SSH_KEY\" \"root@$LXC_HOST\" 'ufw status | grep -cE \"ALLOW\"')\" -ge 3"
    ],
    "checkpoint": "step-04-provision",
    "files": [
      "infra/provision.sh"
    ],
    "status": "pending"
  },
  {
    "id": "E1-T5",
    "title": "Deploy hello-world to production domain over HTTPS",
    "epic": "01-foundation",
    "dependencies": [
      "E1-T4"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN a GET request reaches `https://$APP_DOMAIN/` THE SYSTEM SHALL return HTTP 200.",
      "WHEN the TLS handshake is inspected THE SYSTEM SHALL present a valid certificate chain.",
      "WHEN `http://$APP_DOMAIN/` is requested THE SYSTEM SHALL redirect (301/308) to `https://$APP_DOMAIN/`.",
      "WHEN the LXC's Nginx access log is inspected after a request THE SYSTEM SHALL show it served by PHP-FPM, not a static file."
    ],
    "verify": [
      "set -a; source .env; set +a; test \"$(curl -sS -o /dev/null -w '%{http_code}' https://$APP_DOMAIN/)\" = 200",
      "set -a; source .env; set +a; test \"$(curl -sS -o /dev/null -w '%{http_code}' http://$APP_DOMAIN/)\" -ge 300 -a \"$(curl -sS -o /dev/null -w '%{http_code}' http://$APP_DOMAIN/)\" -lt 400"
    ],
    "checkpoint": "step-05-deploy-hello-world",
    "files": [
      "resources/views/public/hello.blade.php",
      "routes/web.php"
    ],
    "status": "pending"
  },
  {
    "id": "E2-T1",
    "title": "Core identity and profile schema + seeder skeleton",
    "epic": "02-schema-auth",
    "dependencies": [
      "E1-T5"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan migrate` runs against an empty database THE SYSTEM SHALL create `users` and `profiles` tables with the columns in blueprint §4.",
      "WHEN a `User` is created via the Eloquent model THE SYSTEM SHALL persist and retrieve `two_factor_confirmed_at` as null by default.",
      "WHEN `profiles.user_id` is queried THE SYSTEM SHALL enforce uniqueness at the database level.",
      "WHEN `tests/Feature/Schema/CoreIdentityTest.php` runs THE SYSTEM SHALL report 3 passing tests, 0 skipped."
    ],
    "verify": [
      "php artisan migrate:fresh",
      "./vendor/bin/pest tests/Feature/Schema/CoreIdentityTest.php"
    ],
    "checkpoint": "step-06-identity-schema",
    "files": [
      "the migration generated by `php artisan make:migration add_two_factor_columns_to_users_table` (timestamp-prefixed)",
      "the migration generated by `php artisan make:migration create_profiles_table` (timestamp-prefixed)",
      "app/Models/User.php",
      "app/Models/Profile.php",
      "tests/Feature/Schema/CoreIdentityTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E2-T2",
    "title": "Career schema: companies, experiences, education, certifications, skills, technologies",
    "epic": "02-schema-auth",
    "dependencies": [
      "E2-T1"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan migrate` runs THE SYSTEM SHALL create all 7 career-related tables with their documented columns.",
      "WHEN an `Experience` is queried via `Profile::experiences()` THE SYSTEM SHALL return only that profile's rows, ordered by `started_at` desc.",
      "WHEN a duplicate `slug` is inserted into `experiences` THE SYSTEM SHALL raise a unique-constraint violation.",
      "WHEN `tests/Feature/Schema/CareerSchemaTest.php` runs THE SYSTEM SHALL report 7 passing tests, 0 skipped."
    ],
    "verify": [
      "php artisan migrate:fresh",
      "./vendor/bin/pest tests/Feature/Schema/CareerSchemaTest.php"
    ],
    "checkpoint": "step-07-career-schema",
    "files": [
      "the migrations generated by `php artisan make:migration create_{companies,experiences,education,certifications,skills,skill_categories,technologies}_table` (timestamp-prefixed, one file per table)",
      "app/Models/Company.php",
      "app/Models/Experience.php",
      "app/Models/Education.php",
      "app/Models/Certification.php",
      "app/Models/Skill.php",
      "app/Models/SkillCategory.php",
      "app/Models/Technology.php",
      "tests/Feature/Schema/CareerSchemaTest.php"
    ],
    "status": "pending",
    "do": ""
  },
  {
    "id": "E2-T3",
    "title": "Project schema: projects, project_categories, software_projects, pivots",
    "epic": "02-schema-auth",
    "dependencies": [
      "E2-T2"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan migrate` runs THE SYSTEM SHALL create `projects`, `project_categories`, `software_projects`, and the 3 pivot tables.",
      "WHEN a `Project` has a `SoftwareProject` extension row THE SYSTEM SHALL expose it via `Project::softwareProject()` as a `hasOne`.",
      "WHEN a `Project` is attached to a `Technology` via the pivot THE SYSTEM SHALL enforce the composite primary key.",
      "WHEN `tests/Feature/Schema/ProjectSchemaTest.php` runs THE SYSTEM SHALL report 4 passing tests, 0 skipped."
    ],
    "verify": [
      "php artisan migrate:fresh",
      "./vendor/bin/pest tests/Feature/Schema/ProjectSchemaTest.php"
    ],
    "checkpoint": "step-08-project-schema",
    "files": [
      "the migrations generated by `php artisan make:migration create_{projects,project_categories,software_projects,project_technology,project_media,project_documents}_table` (timestamp-prefixed, one file per table)",
      "app/Models/Project.php",
      "app/Models/SoftwareProject.php",
      "app/Models/ProjectCategory.php",
      "tests/Feature/Schema/ProjectSchemaTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E2-T4",
    "title": "Media/document/social/settings/audit/imports schema",
    "epic": "02-schema-auth",
    "dependencies": [
      "E2-T3"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan migrate` runs THE SYSTEM SHALL create `media`, `documents`, `social_profiles`, `settings`, `audit_logs`, `imports`, `import_records`.",
      "WHEN an `AuditLog` row is inserted THE SYSTEM SHALL accept no `updated_at` column write.",
      "WHEN `App\\Contracts\\AnalyticsProviderInterface` is resolved from the container THE SYSTEM SHALL return the `NullAnalyticsProvider` binding.",
      "WHEN a published entity's referenced `Media` row is deleted via `Media::destroy()` THE SYSTEM SHALL throw a named `MediaInUseException` and leave the row intact.",
      "WHEN `tests/Feature/Schema/MediaDocsAuditTest.php` runs THE SYSTEM SHALL report 5 passing tests, 0 skipped."
    ],
    "verify": [
      "php artisan migrate:fresh",
      "./vendor/bin/pest tests/Feature/Schema/MediaDocsAuditTest.php"
    ],
    "checkpoint": "step-09-media-audit-schema",
    "files": [
      "the migrations generated by `php artisan make:migration create_{media,documents,social_profiles,settings,audit_logs,imports,import_records}_table` (timestamp-prefixed, one file per table)",
      "app/Models/Media.php",
      "app/Models/Document.php",
      "app/Models/SocialProfile.php",
      "app/Models/Setting.php",
      "app/Models/AuditLog.php",
      "app/Models/Import.php",
      "app/Models/ImportRecord.php",
      "app/Contracts/PortfolioIntegration.php",
      "app/Contracts/AnalyticsProviderInterface.php",
      "tests/Feature/Schema/MediaDocsAuditTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E2-T5",
    "title": "Auth: Fortify + mandatory TOTP MFA, rate limiting, session expiry, login audit",
    "epic": "02-schema-auth",
    "dependencies": [
      "E2-T4"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN a user without a confirmed `two_factor_confirmed_at` requests `/admin` THE SYSTEM SHALL redirect to the MFA enrolment screen.",
      "WHEN a valid TOTP code is submitted at enrolment THE SYSTEM SHALL set `two_factor_confirmed_at` and display recovery codes exactly once.",
      "WHEN the 6th failed login for the same email+IP occurs within 15 minutes THE SYSTEM SHALL respond 429 and log the attempt.",
      "WHEN a successful login occurs THE SYSTEM SHALL write one `audit_logs` row with `action='login_success'`.",
      "WHEN a failed login occurs THE SYSTEM SHALL write one `audit_logs` row with `action='login_failed'`.",
      "WHEN `tests/Feature/Auth/MfaTest.php` runs THE SYSTEM SHALL report 6 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Auth/MfaTest.php"
    ],
    "checkpoint": "step-10-auth-mfa",
    "files": [
      "app/Actions/Fortify/CreateNewUser.php",
      "app/Http/Middleware/EnsureMfaConfirmed.php",
      "app/Listeners/LogLoginAttempt.php",
      "tests/Feature/Auth/MfaTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E2-T6",
    "title": "Authorization: spatie/laravel-permission, Policies, seeded super_admin role",
    "epic": "02-schema-auth",
    "dependencies": [
      "E2-T5"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan migrate` runs THE SYSTEM SHALL create the spatie permission tables.",
      "WHEN `RolesSeeder` runs THE SYSTEM SHALL create exactly one `super_admin` role and assign it to the seeded admin user.",
      "WHEN a user without `super_admin` calls `Gate::authorize('update', $project)` THE SYSTEM SHALL throw an `AuthorizationException`.",
      "WHEN `tests/Feature/Auth/PolicyTest.php` runs THE SYSTEM SHALL report at least 10 passing assertions, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Auth/PolicyTest.php"
    ],
    "checkpoint": "step-11-authorization",
    "files": [
      "database/seeders/RolesSeeder.php",
      "app/Policies/ProfilePolicy.php",
      "app/Policies/ProjectPolicy.php",
      "app/Policies/ArticlePolicy.php",
      "tests/Feature/Auth/PolicyTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E2-T7",
    "title": "Realistic content seeder + factories for every entity, incl. sample Articles",
    "epic": "02-schema-auth",
    "dependencies": [
      "E2-T6"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan db:seed` runs against a migrated empty database THE SYSTEM SHALL exit 0.",
      "WHEN the seed completes THE SYSTEM SHALL have created exactly 1 row in `users` with the `super_admin` role.",
      "WHEN the seed completes THE SYSTEM SHALL have created at least 2 and at most 3 rows in `articles`, all `published = true`.",
      "WHEN the seed completes THE SYSTEM SHALL have created rows for every entity blueprint §4 defines.",
      "WHEN `tests/Feature/Schema/SeederTest.php` runs THE SYSTEM SHALL report 5 passing tests, 0 skipped."
    ],
    "verify": [
      "php artisan migrate:fresh",
      "php artisan db:seed",
      "./vendor/bin/pest tests/Feature/Schema/SeederTest.php"
    ],
    "checkpoint": "step-12-seeder",
    "files": [
      "database/factories/ProjectFactory.php",
      "database/factories/ArticleFactory.php",
      "database/seeders/DatabaseSeeder.php",
      "tests/Feature/Schema/SeederTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E3-T1",
    "title": "Filament install, panel config, admin user, MFA enforced, Telescope local-only",
    "epic": "03-admin-filament",
    "dependencies": [
      "E2-T7"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `/admin/login` is requested THE SYSTEM SHALL render the Filament login form.",
      "WHEN the seeded `super_admin` logs in with MFA unconfirmed THE SYSTEM SHALL be redirected to MFA enrolment before reaching the dashboard.",
      "WHEN `/telescope` is requested in a non-local environment THE SYSTEM SHALL return 404.",
      "WHEN `tests/Feature/Filament/PanelBootTest.php` runs THE SYSTEM SHALL report 3 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Filament/PanelBootTest.php"
    ],
    "checkpoint": "step-13-filament-install",
    "files": [
      "app/Providers/Filament/AdminPanelProvider.php",
      "tests/Feature/Filament/PanelBootTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E3-T2",
    "title": "Filament resources set 1: Profile, Companies, Experience, Education, Certifications",
    "epic": "03-admin-filament",
    "dependencies": [
      "E3-T1"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN each of the 5 resources' index page is requested by the authenticated `super_admin` THE SYSTEM SHALL return 200.",
      "WHEN a new `Experience` is created through the Filament form THE SYSTEM SHALL persist a row with a non-null `slug` derived from `title`.",
      "WHEN the `published` toggle is switched off THE SYSTEM SHALL cause the entity to disappear from any public listing query.",
      "WHEN `tests/Feature/Filament/ResourceSet1Test.php` runs THE SYSTEM SHALL report 5 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Filament/ResourceSet1Test.php"
    ],
    "checkpoint": "step-14-filament-resources-1",
    "files": [
      "app/Filament/Resources/ProfileResource.php",
      "app/Filament/Resources/CompanyResource.php",
      "app/Filament/Resources/ExperienceResource.php",
      "app/Filament/Resources/EducationResource.php",
      "app/Filament/Resources/CertificationResource.php",
      "tests/Feature/Filament/ResourceSet1Test.php"
    ],
    "status": "pending"
  },
  {
    "id": "E3-T3",
    "title": "Filament resources set 2: Projects (+SoftwareProject relation manager), categories, tech, skills",
    "epic": "03-admin-filament",
    "dependencies": [
      "E3-T2"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `app/Filament/Resources/` is listed THE SYSTEM SHALL contain no file named `SoftwareProjectResource.php`.",
      "WHEN `ProjectResource`'s edit page is requested THE SYSTEM SHALL render a Software Details relation-manager tab.",
      "WHEN a `Project` is attached to `Technology` rows through the Filament form THE SYSTEM SHALL persist the pivot rows.",
      "WHEN `tests/Feature/Filament/ResourceSet2Test.php` runs THE SYSTEM SHALL report 5 passing tests, 0 skipped, including one asserting `SoftwareProjectResource` is not a registered Filament resource class."
    ],
    "verify": [
      "! find app/Filament/Resources -iname \"SoftwareProjectResource.php\" | grep -q .",
      "./vendor/bin/pest tests/Feature/Filament/ResourceSet2Test.php"
    ],
    "checkpoint": "step-15-filament-resources-2",
    "files": [
      "app/Filament/Resources/ProjectResource.php",
      "app/Filament/Resources/ProjectResource/RelationManagers/SoftwareProjectRelationManager.php",
      "app/Filament/Resources/ProjectCategoryResource.php",
      "app/Filament/Resources/TechnologyResource.php",
      "app/Filament/Resources/SkillResource.php",
      "app/Filament/Resources/SkillCategoryResource.php",
      "tests/Feature/Filament/ResourceSet2Test.php"
    ],
    "status": "pending"
  },
  {
    "id": "E3-T4",
    "title": "Media library: hardened upload validation, storage outside web root",
    "epic": "03-admin-filament",
    "dependencies": [
      "E3-T3"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN a `.svg` file is uploaded through any Filament FileUpload field THE SYSTEM SHALL reject it with a validation error, never storing it.",
      "WHEN a file with a spoofed extension is uploaded THE SYSTEM SHALL reject it based on sniffed MIME, not the extension.",
      "WHEN a valid JPEG is uploaded THE SYSTEM SHALL store it re-encoded under `storage/app/private-media/` with a UUID-prefixed filename.",
      "WHEN `/media/{media}/{filename}` is requested for media belonging to an unpublished entity THE SYSTEM SHALL return 404.",
      "WHEN `tests/Feature/Media/UploadValidationTest.php` runs THE SYSTEM SHALL report 5 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Media/UploadValidationTest.php"
    ],
    "checkpoint": "step-16-media-hardening",
    "files": [
      "app/Http/Controllers/Public/MediaController.php",
      "app/Rules/AllowedMediaMime.php",
      "config/filesystems.php",
      "tests/Feature/Media/UploadValidationTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E3-T5",
    "title": "Documents/Resume, Social Profiles, Settings, Audit Log viewer, dashboard widgets",
    "epic": "03-admin-filament",
    "dependencies": [
      "E3-T4"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN a new `Document` version is uploaded for an existing resume THE SYSTEM SHALL increment `version` and keep the prior version's media row intact.",
      "WHEN `/documents/{document}/download` is requested for a published document THE SYSTEM SHALL increment `download_count` by exactly 1 per request.",
      "WHEN a `super_admin` attempts to delete an `AuditLog` row via Filament THE SYSTEM SHALL deny the action.",
      "WHEN the admin dashboard is requested THE SYSTEM SHALL render both widgets with real counts from the database.",
      "WHEN `tests/Feature/Filament/ResourceSet3Test.php` runs THE SYSTEM SHALL report 4 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Filament/ResourceSet3Test.php"
    ],
    "checkpoint": "step-17-admin-remainder",
    "files": [
      "app/Filament/Resources/DocumentResource.php",
      "app/Filament/Resources/SocialProfileResource.php",
      "app/Filament/Resources/SettingResource.php",
      "app/Filament/Resources/AuditLogResource.php",
      "app/Filament/Widgets/RecentActivityWidget.php",
      "tests/Feature/Filament/ResourceSet3Test.php"
    ],
    "status": "pending"
  },
  {
    "id": "E3-T6",
    "title": "Blog admin: Article schema + Filament resource",
    "epic": "03-admin-filament",
    "dependencies": [
      "E3-T5"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan migrate` runs THE SYSTEM SHALL create the `articles` table per blueprint §4's schema.",
      "WHEN an `Article` is created with `published_at` null THE SYSTEM SHALL not appear in `Article::published()` scope results.",
      "WHEN `published_at` is set to a past timestamp THE SYSTEM SHALL cause the article to appear in `Article::published()` results.",
      "WHEN the RichEditor body is saved THE SYSTEM SHALL persist sanitized HTML with script tags stripped.",
      "WHEN `tests/Feature/Filament/ArticleResourceTest.php` runs THE SYSTEM SHALL report 4 passing tests, 0 skipped."
    ],
    "verify": [
      "php artisan migrate",
      "./vendor/bin/pest tests/Feature/Filament/ArticleResourceTest.php"
    ],
    "checkpoint": "step-18-blog-admin",
    "files": [
      "the migration generated by `php artisan make:migration create_articles_table` (timestamp-prefixed)",
      "app/Models/Article.php",
      "app/Filament/Resources/ArticleResource.php",
      "app/Filament/Schemas/HasSeoFields.php",
      "tests/Feature/Filament/ArticleResourceTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E4-T1",
    "title": "Design tokens both themes, Tailwind v4 @theme, self-hosted fonts, base layout, meta component",
    "epic": "04-public-themes",
    "dependencies": [
      "E3-T6"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `npm run build` runs THE SYSTEM SHALL exit 0 and the compiled CSS SHALL contain the `@theme` tokens.",
      "WHEN the base layout renders THE SYSTEM SHALL include a font preload for the 400 and 600 weight IBM Plex Sans files.",
      "WHEN a page uses `<x-meta title=\"...\" description=\"...\">` THE SYSTEM SHALL render a matching `<title>`, `meta[name=description]`, and OG tags.",
      "WHEN `tests/Feature/Layout/MetaComponentTest.php` runs THE SYSTEM SHALL report 3 passing tests, 0 skipped."
    ],
    "verify": [
      "npm run build",
      "test -f public/build/manifest.json",
      "grep -q \"IBM Plex Sans\" resources/css/app.css",
      "./vendor/bin/pest tests/Feature/Layout/MetaComponentTest.php"
    ],
    "checkpoint": "step-19-design-tokens",
    "files": [
      "resources/css/app.css",
      "resources/views/layouts/app.blade.php",
      "resources/views/components/meta.blade.php",
      "public/fonts/ibm-plex-sans-400.woff2",
      "tests/Feature/Layout/MetaComponentTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E4-T2",
    "title": "Theme B (Matrix) + cookie-persisted SSR-correct theme switcher",
    "epic": "04-public-themes",
    "dependencies": [
      "E4-T1"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN the `miautrix_theme` cookie is absent THE SYSTEM SHALL render Theme A and respect `prefers-color-scheme` client-side.",
      "WHEN the cookie is `matrix` THE SYSTEM SHALL render `data-theme=\"matrix\"` on the very first response, before any client JS runs.",
      "WHEN the switcher is used to select Matrix THE SYSTEM SHALL set the cookie and the next page load SHALL be Matrix-themed with no flash.",
      "WHEN the switcher renders in Matrix mode THE SYSTEM SHALL display an 'overrides system theme' label.",
      "WHEN `tests/Feature/Theme/ThemeSwitchTest.php` runs THE SYSTEM SHALL report 4 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Theme/ThemeSwitchTest.php"
    ],
    "checkpoint": "step-20-matrix-theme-switcher",
    "files": [
      "app/Http/Middleware/ResolveTheme.php",
      "resources/views/components/theme-switcher.blade.php",
      "resources/css/app.css",
      "tests/Feature/Theme/ThemeSwitchTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E4-T3",
    "title": "Shared component library rendered correctly in both themes",
    "epic": "04-public-themes",
    "dependencies": [
      "E4-T2"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN each of the 12 components is rendered with `data-theme=\"technical\"` THE SYSTEM SHALL produce output containing no raw hex color in its style attribute.",
      "WHEN each is rendered with `data-theme=\"matrix\"` THE SYSTEM SHALL produce equivalent structural HTML, differing only in resolved token values.",
      "WHEN the `Modal` component opens THE SYSTEM SHALL trap focus within it and return focus to the trigger on close.",
      "WHEN `tests/Feature/Components/ComponentLibraryTest.php` runs THE SYSTEM SHALL report 12 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Components/ComponentLibraryTest.php"
    ],
    "checkpoint": "step-21-component-library",
    "files": [
      "resources/views/components/button.blade.php",
      "resources/views/components/card.blade.php",
      "resources/views/components/modal.blade.php",
      "app/View/Components/Modal.php",
      "tests/Feature/Components/ComponentLibraryTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E4-T4",
    "title": "Public pages: Home, About, Experience, Skills",
    "epic": "04-public-themes",
    "dependencies": [
      "E4-T3"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `/` is requested THE SYSTEM SHALL return 200 and render featured projects and the latest 3 published articles.",
      "WHEN `/about` is requested THE SYSTEM SHALL return 200 and render the profile bio and published experience/education summaries.",
      "WHEN `/experience` is requested THE SYSTEM SHALL return 200 and render only published experiences ordered by `started_at` desc.",
      "WHEN `/skills` is requested THE SYSTEM SHALL return 200 and group skills by skill category.",
      "WHEN `tests/Feature/Public/CorePagesTest.php` runs THE SYSTEM SHALL report 4 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Public/CorePagesTest.php"
    ],
    "checkpoint": "step-22-public-core-pages",
    "files": [
      "resources/views/public/home.blade.php",
      "resources/views/public/about.blade.php",
      "resources/views/public/experience.blade.php",
      "resources/views/public/skills.blade.php",
      "tests/Feature/Public/CorePagesTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E4-T5",
    "title": "Public pages: Projects, Software, Certifications, Resume downloads, Contact",
    "epic": "04-public-themes",
    "dependencies": [
      "E4-T4"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `/projects` is requested THE SYSTEM SHALL return 200, paginated, filterable by category via query param.",
      "WHEN `/projects/{slug}` is requested for a published project THE SYSTEM SHALL return 200 with its technologies, media, and documents.",
      "WHEN `/projects/{slug}` is requested for an unpublished project THE SYSTEM SHALL return 404.",
      "WHEN the contact form's honeypot field is filled THE SYSTEM SHALL silently discard the submission with no mail sent.",
      "WHEN the contact form receives a 6th submission from the same IP within an hour THE SYSTEM SHALL reject with a rate-limit message.",
      "WHEN `tests/Feature/Public/ProjectsContactTest.php` runs THE SYSTEM SHALL report 6 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Public/ProjectsContactTest.php"
    ],
    "checkpoint": "step-23-public-projects-contact",
    "files": [
      "resources/views/public/projects/index.blade.php",
      "resources/views/public/projects/show.blade.php",
      "app/Livewire/ContactForm.php",
      "app/Http/Controllers/Public/ProjectController.php",
      "tests/Feature/Public/ProjectsContactTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E4-T6",
    "title": "Public blog: index + detail + /feed.xml hand-rolled RSS",
    "epic": "04-public-themes",
    "dependencies": [
      "E4-T5"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `/blog` is requested THE SYSTEM SHALL return 200, paginated, showing only published articles.",
      "WHEN `/blog/{slug}` is requested for a published article THE SYSTEM SHALL return 200 with the rendered RichEditor body.",
      "WHEN `/blog/{slug}` is requested for an unpublished article THE SYSTEM SHALL return 404.",
      "WHEN `/feed.xml` is requested THE SYSTEM SHALL return `Content-Type: application/rss+xml` and valid RSS 2.0 XML with one item per published article.",
      "WHEN `tests/Feature/Public/BlogFeedTest.php` runs THE SYSTEM SHALL report 4 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Public/BlogFeedTest.php",
      "php artisan tinker --execute=\"exit(simplexml_load_string(view('feed', ['articles' => App\\Models\\Article::published()->get()])->render()) === false ? 1 : 0);\""
    ],
    "checkpoint": "step-24-public-blog-feed",
    "files": [
      "resources/views/public/blog/index.blade.php",
      "resources/views/public/blog/show.blade.php",
      "resources/views/feed.xml.blade.php",
      "routes/web.php",
      "tests/Feature/Public/BlogFeedTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E5-T1",
    "title": "SEO surface (meta, JSON-LD, sitemap) + A11yTest structural sweep",
    "epic": "05-seo-hardening",
    "dependencies": [
      "E4-T6"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN `php artisan seo:audit` runs against the seeded database THE SYSTEM SHALL exit 0 with 0 missing/duplicate/mismatched fields reported.",
      "WHEN `/sitemap.xml` is requested THE SYSTEM SHALL return 200 with one url entry per published entity including articles.",
      "WHEN `./vendor/bin/pest tests/Feature/SeoTest.php` runs THE SYSTEM SHALL exit 0.",
      "WHEN `./vendor/bin/pest tests/Feature/A11yTest.php` runs over every published route in both `data-theme` values THE SYSTEM SHALL exit 0 with zero structural violations.",
      "WHEN a published `Project` has no `og_image_id` set THE SYSTEM SHALL fall back to a site-default OG image."
    ],
    "verify": [
      "php artisan seo:audit",
      "php artisan serve --port=8123 & SERVE_PID=$!; sleep 1; test \"$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8123/sitemap.xml)\" = 200; kill \"$SERVE_PID\"",
      "./vendor/bin/pest tests/Feature/SeoTest.php",
      "./vendor/bin/pest tests/Feature/A11yTest.php"
    ],
    "checkpoint": "step-25-seo-a11y",
    "files": [
      "app/Console/Commands/SeoAudit.php",
      "tests/Feature/A11yTest.php",
      "tests/Feature/SeoTest.php",
      "resources/views/components/json-ld.blade.php",
      "public/robots.txt"
    ],
    "status": "pending"
  },
  {
    "id": "E5-T2",
    "title": "Database-driven page caching, image optimization, Lighthouse gate",
    "epic": "05-seo-hardening",
    "dependencies": [
      "E5-T1"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN the same public route is requested twice within the cache TTL THE SYSTEM SHALL serve the second request from the database cache.",
      "WHEN a `Project` is published or unpublished in Filament THE SYSTEM SHALL invalidate that project's detail-page cache entry immediately.",
      "WHEN an image is uploaded THE SYSTEM SHALL generate a WebP conversion alongside the original.",
      "WHEN Lighthouse CI runs against Home, one project page, and one article page THE SYSTEM SHALL report Performance >= 95 and SEO >= 95 on all three.",
      "WHEN `tests/Feature/Cache/PageCacheTest.php` runs THE SYSTEM SHALL report 3 passing tests, 0 skipped."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Cache/PageCacheTest.php",
      "set -a; source .env; set +a; npx lighthouse https://$APP_DOMAIN/ --output=json --output-path=/tmp/lh-home.json --chrome-flags=\"--headless\" && jq -e '.categories.performance.score >= 0.95 and .categories.seo.score >= 0.95' /tmp/lh-home.json"
    ],
    "checkpoint": "step-26-cache-performance",
    "files": [
      "app/Actions/Cache/CachePublicPage.php",
      "app/Actions/Cache/InvalidatePublicPageCache.php",
      "tests/Feature/Cache/PageCacheTest.php"
    ],
    "status": "pending"
  },
  {
    "id": "E5-T3",
    "title": "Hardening: headers/CSP, security audit, backup+restore drill, supervised queue, deploy script",
    "epic": "05-seo-hardening",
    "dependencies": [
      "E5-T2"
    ],
    "priority": "p0",
    "acceptance": [
      "WHEN any response is inspected THE SYSTEM SHALL carry `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, and `Referrer-Policy` headers.",
      "WHEN `security-auditor` completes its pass THE SYSTEM SHALL report zero open high-severity findings. (Satisfied by the team Definition of Done — CLAUDE.md's subagent review gate — not by a command in this task's own verify array; there is no automated proxy for a subagent's judgment.)",
      "WHEN the backup script runs and its output is restored to a scratch database THE SYSTEM SHALL match production's row count for every table.",
      "WHEN `supervisorctl status` is queried on the LXC THE SYSTEM SHALL report the `queue-worker` program as `RUNNING`.",
      "WHEN `infra/deploy.sh` is inspected THE SYSTEM SHALL contain neither `migrate:fresh` nor `db:wipe`.",
      "WHEN this task's Checkpoint runs THE SYSTEM SHALL find exactly 27 `step-*` tags in the repository."
    ],
    "verify": [
      "./vendor/bin/pest tests/Feature/Security/HeadersTest.php",
      "test -f infra/deploy.sh && ! grep -q \"migrate:fresh\\|db:wipe\" infra/deploy.sh",
      "ssh -i \"$LXC_SSH_KEY\" \"root@$LXC_HOST\" 'supervisorctl status queue-worker' | grep -q RUNNING",
      "set -a; source .env; set +a; test \"$(curl -s -o /dev/null -w '%{http_code}' https://$APP_DOMAIN/up)\" = 200",
      "bash infra/backup.sh --restore-to-scratch-and-verify"
    ],
    "checkpoint": "step-27-hardening",
    "files": [
      "infra/deploy.sh",
      "infra/backup.sh",
      "app/Http/Middleware/SecurityHeaders.php",
      "tests/Feature/Security/HeadersTest.php"
    ],
    "status": "pending"
  }
]