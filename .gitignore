/.phpunit.cache
/node_modules
/public/build
/public/css/filament
/public/fonts/filament
/public/hot
/public/js/filament
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.env.*.local
!.env.example
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
.DS_Store
Thumbs.db

# Local infrastructure state and generated release artifacts
/infra/.state
/storage/app/private-media
/storage/backups
*.tar.gz

# NOT ignored on purpose — these are committed and the blueprint depends on it:
#   .env.example          (negated above, after the .env* patterns)
#   .claude/              (agent workspace)
#   blueprints/           (the design bundle travels with the code)
#   .github/workflows/    (CI)
#   phpstan.neon, pint.json, phpunit.xml, docker-compose.yml, tests/Pest.php, tests/TestCase.php
